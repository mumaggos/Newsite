# 🎯 CONFIGURAÇÃO SUPER SIMPLES

## O que precisa fazer?

Apenas **3 coisas simples**:

---

## 1️⃣ COPIAR E COLAR NO TERMINAL

Abra o terminal e copie isto (uma linha de cada vez):

```
git clone https://github.com/mumaggos/manusludban.git
```

Depois:

```
cd manusludban
```

Depois:

```
pnpm install
```

---

## 2️⃣ CRIAR UM FICHEIRO

Na pasta do projeto, crie um ficheiro chamado: `.env.local`

Dentro desse ficheiro, copie e cole **isto**:

```
ADMIN_TOKEN=admin-secret-token
PORT=3000
NODE_ENV=development
```

Guarde o ficheiro.

---

## 3️⃣ INICIAR O PROJETO

No terminal, copie e cole:

```
npm run dev
```

Vai aparecer algo como:

```
Local: http://localhost:3000/
```

Copie esse endereço e abra no navegador.

---

## 4️⃣ ACEDER AO PAINEL

No navegador, vá para:

```
http://localhost:3000/admin
```

Clique em "Connect Wallet" e conecte a sua wallet.

---

## 5️⃣ VER OS EMAILS

1. Clique em "Load Subscribers"
2. Clique em "Enter Admin Token"
3. Copie e cole: `admin-secret-token`
4. Clique em "Load"

**Pronto! Agora vê todos os emails registados! ✅**

---

## 📝 O FICHEIRO `.env.local` EXATO

Crie um ficheiro chamado `.env.local` com isto:

```
ADMIN_TOKEN=admin-secret-token
PORT=3000
NODE_ENV=development
```

É isto! Nada mais!

---

## 🎉 FIM!

Só isto! Agora tem tudo a funcionar!
